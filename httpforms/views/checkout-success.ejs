<div>
  <div class="max-w-xl mx-auto px-4 sm:px-6 lg:px-8 py-16 text-center">
    <div class="bg-white rounded-lg shadow-md p-8">
      <div class="flex flex-col items-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 text-green-500 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        
        <h1 class="text-3xl font-bold text-primary mb-4">Payment Successful!</h1>
        
        <p class="text-gray-600 mb-8">Thank you for subscribing to HTTPForms. Your subscription is now active.</p>
        
        <div class="bg-gray-100 rounded-lg p-6 mb-8 w-full">
          <h2 class="text-lg font-bold text-primary mb-2">Subscription Details</h2>
          <div class="grid grid-cols-2 gap-4 text-sm">
            <div class="text-gray-600 text-left">Plan:</div>
            <div class="text-right font-medium"><%= subscription.planType === 'premium' ? 'Premium Plan' : 'Basic Plan' %></div>
            
            <div class="text-gray-600 text-left">Amount:</div>
            <div class="text-right font-medium">$<%= subscription.planType === 'premium' ? '10' : '5' %>/month</div>
            
            <div class="text-gray-600 text-left">Status:</div>
            <div class="text-right font-medium text-green-600">Active</div>
            
            <div class="text-gray-600 text-left">Start Date:</div>
            <div class="text-right font-medium"><%= new Date(subscription.createdAt).toLocaleDateString() %></div>
          </div>
        </div>
        
        <div class="flex flex-col sm:flex-row gap-4">
          <a href="/dashboard" class="btn-primary">Go to Dashboard</a>
          <a href="/account" class="btn-outline">Manage Subscription</a>
        </div>
      </div>
    </div>
  </div>
</div> 